import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  imageContainer: {
    width: "100%",
    height: 300,
    marginBottom: 10,
  },
  image: {
    width: "100%",
    resizeMode: "contain",
  },
});

export default styles;
